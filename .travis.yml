env:
  global:
    - file="issues.txt"
    - issues=$(cat $file);
matrix:
  include:
  - language: bash
    script:
    - echo $PWD 
    - ls -lah
    - bash linux_mac/magpi-issue-downloader.sh -f $issues-4 -l $issues

