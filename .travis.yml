matrix:
  include:
  - language: bash
    script:
    - echo $PWD 
    - ls -lah
    - bash linux_mac/magpi-issue-downloader.sh -f $(cat "/home/travis/build/joergi/MagPiDownloader/issues.txt")-4 -l $(cat "/home/travis/build/joergi/MagPiDownloader/issues.txt")

